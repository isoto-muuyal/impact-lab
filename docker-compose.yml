services:
  impactlab:
    image: ${IMAGE_URI:-impactlab:local}
    container_name: impactlab
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-5000}:5000"
    networks:
      - impactlab-network

networks:
  impactlab-network:
    external: true
    name: ${DOCKER_NETWORK:-shared_network}
